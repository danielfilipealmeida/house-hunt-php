{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="title">New Property</h1>

    <div class="field has-addons">
        <div class="control">
            <a class="button is-small " href=" {{ path("property") }}"><< Back to list</a>
        </div>
    </div>
    
    
    

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}


{% endblock %}

